'use client';

import { ProNetworxDirectoryContent } from '../../DirectoryContent';

export default function ProNetworxDirectoryPage() {
  return (
    <ProNetworxDirectoryContent
      title="Directory"
      subtitle="Find Catalysts by skill, availability, and location."
      showEditProfile
    />
  );
}

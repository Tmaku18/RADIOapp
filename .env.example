# Copy this file to .env at the repo root for docker compose.
# docker compose sets REDIS_URL=redis://redis:6379 for the backend service.
# Fill in your real values; never commit .env.

# --- FIREBASE CONFIGURATION ---
# Option A (recommended): Path to service account JSON in home dir (file is gitignored). No keys in .env.
# Windows: C:\Users\YourName\firebase-service-account.json
# WSL/Docker: set FIREBASE_JSON_PATH to host path so compose can mount it (e.g. /mnt/c/Users/YourName/firebase-service-account.json)
FIREBASE_SERVICE_ACCOUNT_PATH=C:\Users\YourName\firebase-service-account.json
FIREBASE_JSON_PATH=/mnt/c/Users/YourName/firebase-service-account.json
# Option B: Or set project + key inline (get from Firebase Console > Service Accounts)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- SUPABASE CONFIGURATION ---
# Get from Supabase Dashboard > Project Settings > API (service_role key)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# --- STRIPE CONFIGURATION ---
# Get these from Stripe Dashboard > Developers > API Keys
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# --- SERVER CONFIGURATION ---
PORT=3000
NODE_ENV=development
WEB_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3001,http://localhost:3000

# --- ERROR TRACKING ---
# Leave empty to disable
SENTRY_DSN=

# --- REDIS ---
# Overridden by docker-compose.yml (redis://redis:6379) when using Docker
REDIS_URL=redis://redis:6379

# --- RADIO CONFIGURATION ---
THRESHOLD_ENTER_PAID=5
THRESHOLD_EXIT_PAID=3
CHECKPOINT_INTERVAL=5

/// Overlap duration when transitioning between radio tracks (all stations).
/// Archived — not imported by the app. See `core/archive/README.md`.
const int radioCrossfadeMs = 6000;

const Duration radioCrossfadeDuration = Duration(milliseconds: radioCrossfadeMs);

name: radio_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.19+42

environment:
  sdk: ^3.10.0

# App launcher icon (cyan NETWORX branding). Regenerate with:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: "assets/images/branding/networx-logo-cyan.png"
  android: true
  ios: true
  remove_alpha_ios: true
  min_sdk_android: 21
  adaptive_icon_background: "#0A0A0A"
  adaptive_icon_foreground: "assets/launcher/launcher_foreground.png"
  web:
    generate: false
  windows:
    generate: false
  macos:
    generate: false

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  firebase_messaging: ^16.3.0
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^7.0.1
  crypto: ^3.0.6
  
  # Supabase Realtime
  supabase_flutter: ^2.8.0
  
  # Local notifications (for foreground FCM)
  flutter_local_notifications: ^22.0.1

  # Audio playback. We use audio_service (custom NetworxAudioHandler) for the
  # media notification, NOT just_audio_background — the latter replaces
  # just_audio's platform with a single-player implementation and its native
  # plugin competes with audio_service for the media session / audio focus,
  # which garbles playback. Keep it out of the dependency tree entirely.
  just_audio: ^0.10.5
  audio_service: ^0.18.13
  audio_session: ^0.2.3

  # HTTP & API
  http: ^1.2.2
  dio: ^5.7.0

  # File handling
  file_picker: ^11.0.2
  image_picker: ^1.1.2

  # Payments
  flutter_stripe: ^13.0.0
  in_app_purchase: ^3.2.3

  # State management
  provider: ^6.1.2

  # Environment variables
  flutter_dotenv: ^6.0.1

  # UI
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  uuid: ^4.5.1

  # URL launcher for opening web views
  url_launcher: ^6.2.1
  
  # Local storage for settings
  shared_preferences: ^2.2.2
  package_info_plus: ^8.3.0
  geolocator: ^14.0.2
  http_parser: ^4.1.2
  flutter_webrtc: ^1.4.1
  audio_metadata_reader: ^1.6.0

  # 3D butterfly hero is rendered via the real web Three.js scene inside a
  # transparent WebView (System WebView WebGL2 works on physical devices, unlike
  # flutter_angle which times out). three_js kept only as the legacy/emulator path.
  flutter_inappwebview: ^6.1.5
  three_js: ^0.3.0
  flutter_map: ^8.3.1
  latlong2: ^0.10.1

# Vendored three_js renderer: forces flutter_angle's device-native GLES path
# (useAngle:false) so 3D renders on physical Android, not just emulators.
dependency_overrides:
  three_js_angle_renderer:
    path: third_party/three_js_angle_renderer

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.4
  image: ^4.8.0

# Native splash screen (dark NETWORX branding shown before Flutter boots).
# Regenerate after changing with:
#   dart run flutter_native_splash:create
flutter_native_splash:
  color: "#0A0A0A"
  image: assets/images/branding/networx-logo-cyan.png
  android: true
  ios: true
  web: false
  android_12:
    color: "#0A0A0A"
    image: assets/launcher/launcher_foreground.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # NETWORX typography (bundled font assets)
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Variable.ttf
    - family: SpaceGrotesk
      fonts:
        - asset: assets/fonts/SpaceGrotesk-Variable.ttf
    - family: Lora
      fonts:
        - asset: assets/fonts/Lora-Variable.ttf

  # Assets (include .env so flutter_dotenv can load it at runtime on device/emulator)
  assets:
    - .env
    - assets/images/branding/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
